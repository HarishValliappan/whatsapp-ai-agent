Flask==2.1.1
Werkzeug==2.0.3
pywa
httpx==0.19.0  # Use this version for compatibility with pywa
groq
python-dotenv==0.19.2
requests==2.28.1  # Ensure this is installed for HTTP requests
gunicorn
